{
  "$schema": "node_modules/wrangler/config-schema.json",
  "name": "aiflow-landing",
  "compatibility_date": "2025-06-20",
  // Sitio estático (Vite SPA): se sirve la carpeta de build "dist".
  // No hay código de Worker; solo assets estáticos.
  "assets": {
    "directory": "./dist",
    "not_found_handling": "single-page-application"
  }
}
